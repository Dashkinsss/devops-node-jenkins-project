{
  "name": "devops-node-jenkins-project",
  "version": "1.0.0",
  "description": "Node.js DevOps project with Jenkins CI/CD pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "rm -rf dist && mkdir -p dist/routes dist/services dist/data dist/utils && cp app.js server.js dist/ && cp routes/*.js dist/routes/ && cp services/*.js dist/services/ && cp data/*.js dist/data/ && cp utils/*.js dist/utils/",
    "test": "jest --coverage",
    "test:ci": "jest --coverage --ci --reporters=default --reporters=jest-junit",
    "sonar": "npx sonar-scanner"
  },
  "keywords": [
    "devops",
    "jenkins",
    "nodejs",
    "ci-cd",
    "testing"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "sonarqube-scanner": "^3.0.1"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "coverage",
    "coverageReporters": [
      "text",
      "lcov"
    ]
  }
}
